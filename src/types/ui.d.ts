/**
 * UI Constants and styling related type definitions
 */

export type UIConstants = {
  readonly MODAL_Z_INDEX: number;
  readonly ANIMATION_DURATION: number;
  readonly MODAL_BACKDROP_CLASS: string;
  readonly MODAL_CONTENT_CLASS: string;
};

export type FormConstants = {
  readonly SELECT_CLASS: string;
  readonly BUTTON_PRIMARY_CLASS: string;
  readonly BUTTON_SECONDARY_CLASS: string;
};
